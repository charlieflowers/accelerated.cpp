#include <list>
#include <iostream>

#include "charlie_container_type.h"
#include "Index_entry.h"

list<Index_entry> build_index(const container_type& phrases) {
    // Build an index for each phrase. But concatenate them all into one vector, and return that.
    
}

